package Components;

//Only used in Single Cycle / Pipe architectures
public class DataMemory extends MemoryModule{

    public DataMemory(int wordsize, int size, int offset){
        super(wordsize, size, offset);
    }
}
